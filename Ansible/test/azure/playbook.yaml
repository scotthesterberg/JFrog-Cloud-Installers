---
- name: Azure Infra Provision
  hosts: localhost
  tasks:
  - name: Create Azure Deploy
    azure_rm_deployment:
      resource_group: AzureAnsibleResourceGroup
      location: eastus
      name: AzureAnsibleInfra
      parameters:
        vnetName:
          value: "vnetAnsible"
        vnetAddressRange:
          value: "10.0.0.0/16"
        subnetAddressRange:
          value: "10.0.0.0/24"
        location:
          value: "eastus"
        adminUsername:
          value: "ubuntu"
        adminPublicKey:
          value: "{{ lookup('env', 'ssh_public_key') }}"
        sizeOfDiskInGB:
          value: 128
        vmSize:
          value: Standard_D2s_v3
        numberOfArtifactory:
          value: 2
        numberOfXray:
          value: 1
        numberOfDb:
          value: 1
      template_link: 'https://raw.githubusercontent.com/jfrog/JFrog-Cloud-Installers/ansible-rt7/Ansible/infra/azure/lb-rt-xray-ha.json'
    register: azureDeployment
  - name: azure deployment details
    debug:
      var: azureDeployment
  - name: Copy private key to bastion
    shell: "scp -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no -i ~/.ssh/ansible-priv.pem ~/.ssh/ansible-priv.pem ubuntu@{{ azureDeployment.deployment.outputs.lbIp.value }}:.ssh/ansible-priv.pem"
  - name: Add new RT primary to host group
    add_host:
      hostname: "{{ azureDeployment.deployment.outputs.vmArtPriIp.value }}"
      ansible_user: "ubuntu"
      ansible_ssh_private_key_file: "~/.ssh/ansible-priv.pem"
      ansible_ssh_common_args: '-o ProxyCommand="ssh -o StrictHostKeyChecking=no -i ~/.ssh/ansible-priv.pem -W %h:%p ubuntu@{{ azureDeployment.deployment.outputs.lbIp.value }}"'
      db_url: "jdbc:postgresql://{{ azureDeployment.deployment.outputs.vmDbArrIp.value[0] }}:5432/artifactory"
      server_name: "rt.{{ azureDeployment.deployment.outputs.lbIp.value }}.xip.io"
      artifactory_is_primary: true
      artifactory_license1: "{{ lookup('env', 'artifactory_license1') }}"
      artifactory_license2: "{{ lookup('env', 'artifactory_license2') }}"
      artifactory_license3: "{{ lookup('env', 'artifactory_license3') }}"
      artifactory_license4: "{{ lookup('env', 'artifactory_license4') }}"
      artifactory_license5: "{{ lookup('env', 'artifactory_license5') }}"
      groups:
      - artifactory

  - name: Add RT secondaries to host group
    add_host:
      hostname: "{{ item }}"
      ansible_user: "ubuntu"
      ansible_ssh_private_key_file: "~/.ssh/ansible-priv.pem"
      ansible_ssh_common_args: '-o ProxyCommand="ssh -o StrictHostKeyChecking=no -i ~/.ssh/ansible-priv.pem -W %h:%p ubuntu@{{ azureDeployment.deployment.outputs.lbIp.value }}"'
      db_url: "jdbc:postgresql://{{ azureDeployment.deployment.outputs.vmDbArrIp.value[0] }}:5432/artifactory"
      server_name: "rt.{{ azureDeployment.deployment.outputs.lbIp.value }}.xip.io"
      artifactory_is_primary: false
      groups:
      - artifactory
    loop: "{{ azureDeployment.deployment.outputs.vmArtSecArrIp.value }}"

  - name: Add xrays to host group
    add_host:
      hostname: "{{ item }}"
      ansible_user: "ubuntu"
      ansible_ssh_private_key_file: "~/.ssh/ansible-priv.pem"
      ansible_ssh_common_args: '-o ProxyCommand="ssh -o StrictHostKeyChecking=no -i ~/.ssh/ansible-priv.pem -W %h:%p ubuntu@{{ azureDeployment.deployment.outputs.lbIp.value }}"'
      jfrog_url: "http://rt.{{ azureDeployment.deployment.outputs.lbIp.value }}.xip.io"
      master_key: "{{ lookup('env', 'master_key') }}"
      join_key: "{{ lookup('env', 'join_key') }}"
      db_type: "postgresql"
      db_driver: "org.postgresql.Driver"
      db_user: "xray"
      db_password: "xray"
      db_url: "postgres://{{ azureDeployment.deployment.outputs.vmDbArrIp.value[0] }}:5432/xraydb?sslmode=disable"
      groups: xray
    loop: "{{ azureDeployment.deployment.outputs.vmXrayArrIp.value }}"

  - name: Add DBs to host group
    add_host:
      hostname: "{{ item }}"
      ansible_user: "ubuntu"
      ansible_ssh_private_key_file: "~/.ssh/ansible-priv.pem"
      ansible_ssh_common_args: '-o ProxyCommand="ssh -o StrictHostKeyChecking=no -i ~/.ssh/ansible-priv.pem -W %h:%p ubuntu@{{ azureDeployment.deployment.outputs.lbIp.value }}"'
      db_users:
        - { db_user: "artifactory", db_password: "Art1fAct0ry" }
        - { db_user: "xray", db_password: "xray" }
      dbs:
        - { db_name: "artifactory", db_owner: "artifactory" }
        - { db_name: "xraydb", db_owner: "xray" }
      groups: database
    loop: "{{ azureDeployment.deployment.outputs.vmDbArrIp.value }}"
  - debug:
      msg: "Unified URL is at http://rt.{{ azureDeployment.deployment.outputs.lbIp.value }}.xip.io"

- hosts: database
  roles:
  - postgres

- hosts: artifactory
  vars:
    artifactory_ha_enabled: true
    master_key: "{{ lookup('env', 'master_key') }}"
    join_key: "{{ lookup('env', 'join_key') }}"
    db_download_url: "https://jdbc.postgresql.org/download/postgresql-42.2.12.jar"
    db_type: "postgresql"
    db_driver: "org.postgresql.Driver"
    db_user: "artifactory"
    db_password: "Art1fAct0ry"
  roles:
  - artifactory

- hosts: xray
  roles:
  - xray